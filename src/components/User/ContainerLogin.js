import React from 'react'

const ContainerLogin = () => {
    return (
        <div>
            {/* {(!User.id) ? onLogon() : <Redirect to="/" />} //lOGIN */ }
            {/* { (!User.id) ? onLogin() : onConnect()}      NEWlOGIN */}
        </div>
    )
}

export default ContainerLogin
